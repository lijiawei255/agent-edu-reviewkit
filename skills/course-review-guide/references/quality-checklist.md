# 质量检查清单（36项）

生成HTML复习文档后，逐项自检。标记为🔴的项为强制必须通过。

## 结构与导航

- [ ] 🔴 1. 文档包含完整8部分：Hero → 阅读指南 → 目录 → 第〇章 → 各章节 → 附录ABC
- [ ] 🔴 2. Hero封面包含：考试范围、排除章节、考试形式、教师、教材、日期
- [ ] 3. 侧边栏目录有进度checkbox + 进度条（宽屏≥1200px固定显示）
- [ ] 4. 内联目录（窄屏替代，含所有章节锚点链接）
- [ ] 5. 所有目录锚点链接与章节id一一对应，可正常跳转
- [ ] 6. 主内容区顶部有搜索栏，可搜索所有内容块

## 内容质量

- [ ] 🔴 7. 每个📌核心概念有完整的5层结构：定义→配图→物理意义→数学拆解→适用条件
- [ ] 🔴 8. 每个数学符号第一次出现时说明了含义、单位、取值范围
- [ ] 🔴 9. 每章至少8-10张课件原图，图片说明与上下文结合（不仅仅是"如图所示"）
- [ ] 🔴 10. 每个核心推导有完整步骤 + 每步"为什么这一步？"解释，不少于5步
- [ ] 🔴 11. 每章至少1道综合性计算大题（含考点分析→解题策略→分步详解→验证→易错点）
- [ ] 12. 第〇章包含课程主线、核心关系、学习动机、章节关系图
- [ ] 13. 阅读指南有针对不同基础学生的复习路径建议

## 公式与数学排版

- [ ] 🔴 14. 所有公式使用LaTeX，重要结果用 `\boxed{}`
- [ ] 🔴 15. 逐一核对每个公式的完整性——无断裂、符号丢失、上下标错位
- [ ] 🔴 16. `$$` 独立公式块成对出现（偶数个`$$`）
- [ ] 🔴 17. `$$` 必须单独成行，公式内容不与`$$`同行
- [ ] 18. 行内公式用 `$...$`，中文说明放在公式块外
- [ ] 19. 函数名统一使用标准LaTeX：`\sin`、`\cos`、`\lim`、`\exp` 等

## 图片与视觉

- [ ] 🔴 20. 所有图片在嵌入前已完成 Phase 2.5 三步视觉交叉验证：
  - 第一步：PIL尺寸筛查（排除宽高比>4或高度<60px的装饰元素）
  - 第二步：视觉识别（MCP工具或模型视觉，逐张识别实际内容）
  - 第三步：交叉比对（视觉结果 vs 页面文字上下文，分类为 ✅匹配/❌装饰/⚠️标题修正）
- [ ] 🔴 21. 验证报告在上下文中可追溯——明确列出每张图片的验证结论
- [ ] 🔴 22. 已运行 `embed_images.py` 将所有图片base64内嵌到HTML
- [ ] 🔴 23. 所有 `<img>` 引用路径在嵌入前正确可访问
- [ ] 🔴 24. 图片图说（figcaption）准确描述实际图片内容——视觉验证中标记为"⚠️标题修正"的已全部修正
- [ ] 🔴 25. 课件图片缺失时使用SVG示意图替代（禁止使用ASCII字符画）。SVG内联在HTML中，设置viewBox，文本使用 `<text>` 元素
- [ ] 26. 图片选择符合优先级：概念示意图 > 推导图 > 对比图 > 流程图 > 例题配图 > 数据图
- [ ] 🔴 27. 不引用的图片已过滤：装饰渐变条(尺寸筛查标记)、Logo、公式截图、<200×200px、页眉页脚、视觉识别与文字不匹配的图片
- [ ] 🔴 28. 确认HTML中无装饰性图片残留：运行 `grep -c "data:image" 文档.html` 得到的数字应 ≤ 视觉验证中标记为"✅匹配"的图片总数

## 交互组件

- [ ] 🔴 29. 每章有选项卡布局（⚡快速复习 + 📖详细讲解），默认显示快速复习
- [ ] 🔴 30. 每章快速复习面板有3-5张术语闪卡（`.flashcard-grid`）
- [ ] 🔴 31. 搜索HTML确认：所有闪卡DIV中**无内联onclick属性**
- [ ] 🔴 32. 搜索HTML确认：无 `onclick="...toggle('flipped')..."` 代码
- [ ] 33. 每章有2-4道练习题的 `.quiz-section`，答案默认折叠
- [ ] 34. 每章推导步骤使用 `<details class="derive-steps">` 可折叠

## 交互功能

- [ ] 35. 进度checkbox在localStorage中持久化，刷新后恢复
- [ ] 36. 搜索栏可实时过滤所有内容块（章节卡片、阅读指南、目录）
- [ ] 37. 闪卡点击可正常翻转（事件委托，无双重翻转bug）
- [ ] 38. 选项卡切换正常

## 最终检查

- [ ] 🔴 39. 搜索全文档确认无作者姓名（Li Jiawei, Peng Chen, Cai Haoxuan）
- [ ] 🔴 40. MathJax使用CDN在线渲染（`https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-svg.js`），HTML中无任何本地 `./mathjax/` 或 `../mathjax/` 路径引用
- [ ] 41. 移动端（≤768px）和打印样式均可用
- [ ] 42. 打印时交互元素正确降级（所有内容可见，无控件干扰）
- [ ] 43. JavaScript为单内联 `<script>` 块，无外部依赖

## 快速诊断命令

生成文档后在终端运行以下检查：

```bash
# === 图片质量检查 ===
# 检查嵌入图片数量（应=视觉验证中✅匹配的数量，不含装饰图）
grep -c "data:image" 文档.html

# 检查装饰性图片是否已清除（7019x1等尺寸在HTML中不会被直接检测到，
# 但可在嵌入前用Python批量筛查——参见Phase 2.5第一步）
python -c "
from PIL import Image
import os, re, base64
# 从HTML提取base64图片并分析尺寸
"

# === JS/CSS检查 ===
# 检查闪卡bug（应无输出）
grep -c "onclick" 文档.html

# 检查JS引号bug——addEventListener中不应出现双单引号（应无输出）
grep -c "''click''\|''input''\|''.tab-btn''\|''.flashcard''\|''active''" 文档.html

# 检查ASCII art残留（应无输出）
grep -c "┌\|└\|├\|│\|┐\|┘\|┤" 文档.html

# === 公式与数学检查 ===
# 检查公式完整（应为偶数）
grep -o '$$' 文档.html | wc -l

# 检查MathJax是否纯CDN（应无本地mathjax引用）
grep -c "\./mathjax\|\.\./mathjax" 文档.html

# === 内容合规检查 ===
# 检查作者名泄露（应无输出）
grep -i "jiawei\|peng.chen\|haoxuan" 文档.html
```
